<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hyung.gwang.eyers2">


    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_logo_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">

        <activity android:name=".login.RegisterActivity"></activity>

        <activity android:name=".MainActivity"></activity>

        <activity android:name=".scan.ScanIdActivity"></activity>
        <activity android:name=".scan.ScanPwActivity"></activity>

        <activity android:name=".view.NoticeActivity"/>
        <activity android:name=".view.PageTestTwo"/>
        <activity android:name=".view.PageTestThree"/>
        <activity android:name=".view.PageTestFour"/>
        <activity android:name=".view.AdminActivity"/>
        <activity android:name=".view.AdminWriteActivity"/>

        <activity android:name=".detail.NoticeDetailActivity"/>

        <uses-library android:name="org.apache.http.legacy" android:required="false" />

        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>